[workspace]
authors = ["KiviCode <kivicode.dev@gmail.com>"]
channels = [
    "https://conda.modular.com/max",
    "conda-forge",
    "https://repo.prefix.dev/modular-community",
]
name = "protoc-gen-mojo"
platforms = ["osx-arm64", "linux-64", "linux-aarch64"]
description = "protoc plugin that generates Mojo structs + gRPC stubs from .proto files."
license = "MIT"
license-file = "LICENSE"
readme = "README.md"
repository = "https://github.com/kivicode/mo_grpc"
preview = ["pixi-build"]

[package]
name = "protoc-gen-mojo"
version = "0.1.0"

[package.build]
backend = { name = "pixi-build-mojo", version = "*" }

[package.build.config]
bins = [
    { name = "protoc_gen_mojo", extra-args = ["-I", "$PREFIX/lib/mojo"] },
]

[dependencies]
mojo = ">=0.26.2.0,<0.26.3.0"
mo_protobuf = { path = "../mo_protobuf" }

[package.build-dependencies]
mojo-compiler = ">=0.26.2.0,<0.26.3.0"

[package.host-dependencies]
mojo-compiler = ">=0.26.2.0,<0.26.3.0"
mo_protobuf = { path = "../mo_protobuf" }

[package.run-dependencies]
mojo-compiler = ">=0.26.2.0,<0.26.3.0"
mo_protobuf = { path = "../mo_protobuf" }

[tasks]
build = { cmd = "pixi build -o ." }
